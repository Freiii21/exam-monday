import s from './SettingsButton.module.css'

export const SettingsButton = () => {
    return (
        <div>
            <button className={s.set}>
                set
            </button>
        </div>

    )
}